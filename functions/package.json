{
  "name": "functions",
  "description": "Cloud Functions for the nonogram app: phraseHint (item 7.6) turns a solver deduction into player-facing hint text via an LLM call, keeping the API key server-side; createPairingCode/redeemPairingCode (item 4) back cross-device stats pairing via Anonymous Auth custom tokens.",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  }
}
